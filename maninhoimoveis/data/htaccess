# Impede que qualquer pessoa acesse os arquivos JSON diretamente pela URL
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
